- name: print variables from inventory
  hosts: local
  connection: local
  tasks:
  - name: Print course info
    ansible.builtin.debug:
      msg: " Learning {{ Course }}, on my {{ Trainer }}, in {{ Friend }} laptop"