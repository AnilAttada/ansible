- name: Play Level Variables
  hosts: local
  connection: local
  vars:
    Course: ansible
    Duration: 10 hrs
    Trainer: self
  tasks:
  - name: Print vars
    ansible.builtin.debug:
      msg: "Learning {{ Course }}, With {{ Duration }} duration, by {{ Trainer }}"